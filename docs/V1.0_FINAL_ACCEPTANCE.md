# BuildCost Pro V1.0 — Final Acceptance Evidence

## Release baseline

- Repository: `mongkol-ux/BuildCost-Pro`
- Default branch: `main`
- Production URL: `https://buildcost-pro-production.up.railway.app`
- Release hardening baseline: merged into `main`
- Production operations baseline: merged into `main`
- Railway healthcheck reliability fix: merged into `main`

## Production verification contract

The production release candidate workflow validates:

1. PostgreSQL 16 startup and schema migration.
2. Production application import.
3. Unit and integration tests.
4. Python source compilation.
5. Public `/health` response contract.
6. Production security headers and HSTS.

The production operations monitor independently checks the public production health contract and security headers every 30 minutes and on pushes to `main`.

## Railway verification state

The active Railway deployment for `buildcost-pro-production.up.railway.app` has been observed as successful in the production project UI. A historical Railway deployment associated with the healthcheck-fix commit was also observed as failed, while the current deployment is active and successful.

This document records the distinction between historical deployment status and current production runtime status. It does not treat a historical failure as a release pass.

## Final release gate

V1.0 is considered fully accepted only after:

- the current `main` commit has current green CI/deployment evidence;
- production `/health` and security-header checks pass;
- the `main` branch protection policy is enabled; and
- immutable V1.0 release evidence is recorded.

Until all gates are green, the release decision remains `HOLD`.
